pkg-example-maps
================

This is an example Joomla package comprising 

- a module which displays a map
- a plugin which handles the map API key  

The module can be displayed several times on the same web page, 
each with different configurations,
and the plugin handles the API key which is configured just once.